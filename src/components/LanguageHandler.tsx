interface LanguageHandlerProps {
  locale: string;
}

export default function LanguageHandler({ locale }: LanguageHandlerProps) {
  return null; // This component can be used for language-related effects later
} 